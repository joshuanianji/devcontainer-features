{
    "name": "Github CLI Persistence",
    "id": "github-cli-persistence",
    "version": "0.0.0",
    "description": "Preserves `~/.config/gh` folder across container instances, to avoid extra logins after container restarts",
    "options": {},
    "mounts": [
        {
            "source": "${devcontainerId}-gh-cli",
            "target": "/dc/github-cli",
            "type": "volume"
        }
    ],
    "installsAfter": [
        "ghcr.io/devcontainers/features/github-cli"
    ]
}